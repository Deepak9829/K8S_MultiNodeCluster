image: hashicorp/terraform

pipelines:
  default:
      - step:
            name: Test
            script:
              - ls -la
              - terraform init
              - terraform validate
              - terraform plan
  
