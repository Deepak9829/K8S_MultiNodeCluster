image: hashicorp/terraform

pipelines:
  default:
      - step:
            name: Test
            script:
              - ls -la
              - chdir EC2
              - export AWS_ACCESS_KEY_ID=$ACCESS_KEY
              - export AWS_SECRET_ACCESS_KEY=$SECRET_KEY
              - terraform init
              - terraform validate
              - terraform plan
              - terraform fmt
  
